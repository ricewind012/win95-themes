[class*="contextmenu_"] {
	/{attr(contextMenu)} {
		@extend body;

		min-width: unset;
	}

	/{attr(contextMenuContents)} {
		background: var(--color-button-face);
	}

	/{attr(contextMenuItem)} {
		color: var(--color-button-text);
		font: var(--text-ui);
		border: none;

		padding: 1px 20px;

		&:hover,
		&[class*="active"] {
			background-color: var(--color-highlight);
			color: var(--color-highlight-text);
		}
	}
}

[class*="rootmenu_"] {
	// TODO radio ?
	/{attr(Icon)} {
		background-image: var(--control-img);

		position: absolute;
		left: 6px;
		box(7px);

		:hover > & {
			filter: invert();
		}

		&:not(:empty) {
			@extend input[type="checkbox"]:checked;
		}

		& > svg {
			display: none;
		}
	}
}

[class*="titlebarcontrols_"] {
	/{attr(LogoutButton)} {
		white-space: pre;

		gap: 0;
	}

	/{attr(AccountName)},
	/{attr(WalletBalance)} {
		color: inherit;
	}

	/{attr(NotificationsMenu)} {
		color: var(--color-button-text);
		border: none;
		box-shadow: none;

		padding: 0;

		width: unset;
	}

	/{attr(NotificationHeader)} {
		padding: 8px;
		padding-right: 2px;
	}

	/{attr(AllNotificationsTitle)} {
		font: var(--text-ui);
	}

	/{attr(EmptyNotificationsCtn)} {
		@extend body;
	}

	/{attr(EmptyNotificationsBody)} {
		color: inherit;
	}
}
