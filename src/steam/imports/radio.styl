#Group,
#Button {
	background-color: transparent;
	padding: 0;
}

#Group {
	box-shadow: none;
	gap: 4px;
	flex-direction: column;
}

#Button {
	@extend label;
	@extend input[type="radio"];

	font: var(--text-ui);
	color: var(--color-button-text);
	text-align: left;
	gap: 0;

	width: unset;
	flex-direction: row;
	justify-content: left;

	&::before,
	&::after {
		content: '';
	}

	// Use as ::before for label spacing
	&::after {
		width: calc(var(--spacing-label) + 13px);
		order: -1;
	}

	&#Active {
		@extend input[type="radio"]:checked;
	}

	& > span {
		parentheses();
	}
}
