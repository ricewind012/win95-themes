#TextToolTip,
#ToolTipCustom {
	@extend .tooltip;

	font-size: var(--text-ui-size);
	box-shadow: none;
}

#ToolTipCustom {
	gap: 1em;

	display: flex;
	flex-direction: column;
}

#ToolTipTitle,
#ToolTipInsetContent {
	color: inherit;
	margin: 0;
}

