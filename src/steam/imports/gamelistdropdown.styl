#Bar {
	padding: 0;

	circle {
		stroke: var(--color-button-text);
	}

	svg {
		fill: var(--color-button-text);
	}
}

#MenuHeader {
	@extend select;

	cursor: default;
}

#ScrollToTop,
#Arrow {
	display: none;
}

#ViewFiltersBar {
	margin: 0;
	margin-right: 3px;
}

#CheckboxWithImage {
	@extend $small-button;

	&::before,
	&::after {
		display: none;
	}

	&[class*="Active"] {
		@extend button:active;
	}
}
