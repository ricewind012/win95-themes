#Field {
	--field-row-children-spacing: 7px;

	background-color: transparent;
	padding: 0;

	div:not([class]) > &:not([class*="settings_OverlayTabSetting_"]) {
		@extend fieldset;

		gap: var(--text-spacing);

		display: flex;
		flex-direction: column;
	}

	&::after { // lol
		display: none;
	}
}

#FieldChildren {
	gap: var(--text-spacing);
}

#FieldIcon {
	display: none;
}

#FieldLabel {
	font: var(--text-ui);
	color: var(--color-button-text);

	& > :not([class]) { // has something else
		gap: 1ch;

		display: flex;
		align-items: center;
	}
}

#FieldLabelRow {
	flex-direction: row-reverse;
}

#FieldDescription {
	#WithDescription > #Row + & {
		color: var(--color-gray-text);
		margin-left: calc(var(--sel-button-size) + var(--label-spacing));
	}
}

#ControlsListOuterPanel {
	--controls-list-spacing: var(--label-spacing);

	gap: 0;

	flex: unset;
}

#ControlsListChild {
	max-width: unset;
}

#Toggle {
	@extend input;
	@extend input[type="checkbox"];

	&#On {
		@extend input[type="checkbox"]:checked;
	}
}

#ToggleRail,
#ToggleSwitch {
	display: none;
}
