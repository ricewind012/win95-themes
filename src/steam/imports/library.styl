#ContextMenuAction {
	@extend .menu-entry[primary];

	background-image: none;
	text-transform: unset;
	letter-spacing: 0;
	border-radius: 0;
}

#LibraryWindowDivider {
	background-color: transparent;

	width: 8px;
}

#MainPanel {
	--tab-height: 18px;
	margin-top: var(--tab-height);
	margin-bottom: 2px;
}

#AppDetailsTransitionGroup {
	@extend [role="tabpanel"];
}

#AppDetailsMain {
	@extend .content-container;

	inset: 3px 4px 4px 3px;
}
