#PageableContainer {
	@extend fieldset;

	svg {
		color: inherit;
	}

	polyline {
		stroke: var(--color-window-text);
	}
}

#HeaderPageControls {
	font: var(--text-ui);
	color: inherit;
}

#Name {
	@extend $legend-as-div;

	background-color: var(--color-window);

	letter-spacing: 0;
}
