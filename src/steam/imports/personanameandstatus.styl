#playerName {
	@extend $text-reset;

	font-weight: bold;
	margin-bottom: -2px;
}

#richPresenceLabel {
	@extend $text-reset;

	color: var(--color-gray-text);
	margin-top: -2px;
}

#statusAndName {
	gap: var(--text-spacing);
	margin-top: 1px;
	margin-bottom: 0;
}

