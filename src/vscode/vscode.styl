codicon() {
	font-family: codicon $i;
	font-size: 11px $i;
}

replace-icon(w, h = w) {
	&::before {
		-webkit-mask: var(--icon-img) no-repeat;
		-webkit-mask-size: w h;
		-webkit-mask-position: var(--icon-img-pos, center);
		background-color: var(--color-button-text);

		width: w $i;
		height: 100% $i;

		display: block;
	}
}

@import '../shared/*';

@import 'extensions/*';
@import 'global/*';
@import 'parts/*';
@import 'pages/*';
@import 'popup/*';
