.suggest-widget {
	@extend body;

	border: none;
	border-radius: 0;

	.monaco {
		&-scrollable-element {
			overflow: hidden;
		}

		&-list-row {
			padding-right: var(--scrollbar-size);

			&s {
				margin: 0;
			}
		}
	}
}
