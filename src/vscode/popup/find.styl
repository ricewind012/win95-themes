.find-widget {
  @extend body;

  border-radius: 0;

  height: auto;

  // Horrible layout, kill yourself
  .button {
    @extend $small-button;

    inset: 0;

    &.codicon-widget-close {
      left: 16px;
      top: -39px;
    }
  }

  .find-part,
  .replace-part {
    font();

    margin: 0;
  }
}