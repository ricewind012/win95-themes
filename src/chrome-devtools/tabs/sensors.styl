.latlong-group {
	gap: var(--label-spacing);
	margin-bottom: 8px;
}

.sensors {
	&-group {
		@extend fieldset;

		&-title {
			@extend $legend-as-div;
		}
	}

	&-view {
		fieldset[disabled] {
			opacity: 1;
		}
	}
}
